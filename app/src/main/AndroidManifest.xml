<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.polytech.repo_ihm">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Repoihm.NoActionBar"
        android:usesCleartextTraffic="true">
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <activity
            android:name=".StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".activities.AlertePeremptionActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ideeRecettes.IdeeRecettesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AideDosageActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MesInventairesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MarketPlaceActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />




        <activity
            android:name=".account.LoginActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".account.RegisterActivity"
            android:exported="false">
        </activity>
        <!-- android:apikey="Y2VjaSBlc3QgdW5lIHBhdGF0ZSBkb3VjZQ==" -->
        <meta-data
            android:name="com.google.android.geo.AIzaSyCpCBJSNRMiWM2fJuSz7jrK5rZopBcfptQ"
            android:value="AIzaSyCpCBJSNRMiWM2fJuSz7jrK5rZopBcfptQ" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
    </application>

</manifest>